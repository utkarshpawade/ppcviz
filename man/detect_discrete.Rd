% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/detect.R
\name{detect_discrete}
\alias{detect_discrete}
\title{Detect Discreteness or Point Masses in Data}
\usage{
detect_discrete(y, threshold = 0.02)
}
\arguments{
\item{y}{Numeric vector of observed data.}

\item{threshold}{Relative frequency threshold (default 0.02 = 2\%). Any value
appearing more than this fraction of the time is flagged.}
}
\value{
An S3 object of class \code{"discrete_check"} with components:
\describe{
\item{\code{is_discrete}}{Logical. TRUE if any value exceeds \code{threshold}.}
\item{\code{is_mixed}}{Logical. TRUE if data appears to be a mixture of
continuous bulk and discrete point masses (some values flagged but
many unique values exist).}
\item{\code{n_unique}}{Number of unique values.}
\item{\code{n}}{Total number of observations.}
\item{\code{unique_ratio}}{Ratio of unique values to total observations.}
\item{\code{flagged_values}}{Named numeric vector of values exceeding
\code{threshold}, with their relative frequencies as values.}
}
}
\description{
Applies the heuristic from Section 2.2 of Sailynoja et al. (2025): if any
value has a relative frequency greater than \code{threshold}, the data is flagged
as potentially discrete or containing point masses (e.g., zero-inflation).
}
\examples{
# Continuous data — should not flag anything
detect_discrete(rnorm(200))

# Discrete counts
detect_discrete(rpois(200, lambda = 3))

# Zero-inflated: mixture of zeros and continuous values
y_mixed <- c(rep(0, 60), rnorm(140, mean = 2))
detect_discrete(y_mixed)

}
\references{
Sailynoja, T., Johnson, A., Martin, R., & Vehtari, A. (2025).
Recommendations for visual predictive checks in Bayesian workflow.
arXiv:2503.01509.
}

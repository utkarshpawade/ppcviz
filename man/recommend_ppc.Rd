% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/recommend.R
\name{recommend_ppc}
\alias{recommend_ppc}
\title{Automatic PPC Plot Recommender}
\usage{
recommend_ppc(y, yrep = NULL)
}
\arguments{
\item{y}{Numeric vector of observed data.}

\item{yrep}{Optional numeric matrix of posterior predictive draws (rows =
draws, columns = observations). If provided and \code{y} is continuous, runs
\code{\link[=check_viz]{check_viz()}} to test whether a KDE is appropriate.}
}
\value{
An S3 object of class \code{"ppc_recommendation"} with components:
\describe{
\item{\code{recommended}}{Character vector of recommended function names.}
\item{\code{avoid}}{Character vector of functions to avoid.}
\item{\code{reason}}{Character scalar explaining the recommendation.}
\item{\code{data_type}}{One of \code{"continuous"}, \code{"discrete"}, \code{"mixed"},
\code{"binary"}.}
\item{\code{bounds}}{Detected boundary information (a \code{bounds_check} object).}
\item{\code{discrete}}{Discreteness detection result (a \code{discrete_check}
object).}
\item{\code{kde_gof}}{A \code{viz_gof} object if KDE was tested, otherwise \code{NULL}.}
}
}
\description{
Examines data properties (discreteness, boundaries) and optionally tests
whether a KDE visualization is faithful to the data, then returns a
data-driven recommendation for which posterior predictive check (PPC) plot
function to use. Recommendations follow the decision logic from Sailynoja
et al. (2025).
}
\examples{
set.seed(42)

# Continuous unbounded
recommend_ppc(rnorm(300))

# Non-negative (exponential)
recommend_ppc(rexp(300))

# Proportions
recommend_ppc(rbeta(300, 2, 5))

# Discrete counts
recommend_ppc(rpois(300, lambda = 3))

# Zero-inflated
recommend_ppc(c(rep(0, 80), rnorm(220, mean = 2)))

# Binary
recommend_ppc(sample(0:1, 300, replace = TRUE))

}
\references{
Sailynoja, T., Johnson, A., Martin, R., & Vehtari, A. (2025).
Recommendations for visual predictive checks in Bayesian workflow.
arXiv:2503.01509.
}
\seealso{
\code{\link[=detect_discrete]{detect_discrete()}}, \code{\link[=detect_bounds]{detect_bounds()}}, \code{\link[=check_viz]{check_viz()}},
\code{\link[=ppc_calibration]{ppc_calibration()}}
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/detect.R
\name{detect_bounds}
\alias{detect_bounds}
\title{Detect Natural Bounds in Data}
\usage{
detect_bounds(y, tol = 0.01)
}
\arguments{
\item{y}{Numeric vector of observed data.}

\item{tol}{Relative tolerance for boundary detection (default 0.01). Controls
how much "room" must exist between the data range and the detected boundary.}
}
\value{
An S3 object of class \code{"bounds_check"} with components:
\describe{
\item{\code{lower}}{Detected lower bound (numeric), or \code{NULL} if none found.}
\item{\code{upper}}{Detected upper bound (numeric), or \code{NULL} if none found.}
\item{\code{likely_non_negative}}{Logical. TRUE if data is all non-negative
and a lower bound near 0 is detected.}
\item{\code{likely_proportion}}{Logical. TRUE if data appears bounded to
\eqn{[0, 1]}.}
}
}
\description{
Checks whether continuous data has natural lower and/or upper bounds by
examining the minimum and maximum values and the density near those extremes.
A sharp density cutoff near the data range suggests a hard boundary.
}
\examples{
# Unbounded continuous data
detect_bounds(rnorm(300))

# Non-negative data (e.g., exponential)
detect_bounds(rexp(300))

# Proportion data bounded to [0, 1]
detect_bounds(rbeta(300, 2, 5))

}
\references{
Sailynoja, T., Johnson, A., Martin, R., & Vehtari, A. (2025).
Recommendations for visual predictive checks in Bayesian workflow.
arXiv:2503.01509.
}

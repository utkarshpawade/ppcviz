% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calibration.R
\name{ppc_calibration_residual}
\alias{ppc_calibration_residual}
\title{PAVA Calibration Residual Plot}
\usage{
ppc_calibration_residual(
  y,
  yrep,
  x = NULL,
  n_quantiles = 10L,
  prob = 0.9,
  n_boot = 500L,
  ...
)
}
\arguments{
\item{y}{Integer or logical vector of observed binary outcomes (0/1 or
FALSE/TRUE). Length \code{n}.}

\item{yrep}{Numeric matrix of posterior predictive draws. Rows = draws,
columns = observations. Dimensions: \verb{S x n} where \code{S} is the number of
posterior draws.}

\item{x}{Optional numeric vector of length \code{n}. If provided, used as the
x-axis variable instead of predicted probabilities. Useful for visualising
calibration as a function of a covariate.}

\item{n_quantiles}{Integer. Number of quantile bins for grouping predicted
probabilities before applying isotonic regression (default 10). More bins
give finer resolution but noisier estimates.}

\item{prob}{Nominal coverage probability for the consistency intervals
(default 0.9).}

\item{n_boot}{Integer. Number of bootstrap resamples for the consistency
intervals (default 500).}

\item{...}{Currently unused; reserved for future arguments.}
}
\value{
A \code{ggplot} object.
}
\description{
Similar to \code{\link[=ppc_calibration]{ppc_calibration()}}, but plots residuals (CEP - predicted
probability) on the y-axis so that perfect calibration is a horizontal line
at zero. Optionally uses a covariate \code{x} on the x-axis to diagnose
covariate-specific miscalibration.
}
\examples{
set.seed(42)
n <- 200
true_p <- stats::plogis(rnorm(n))
y <- stats::rbinom(n, 1, true_p)
yrep <- do.call(rbind, lapply(seq_len(100), function(i) {
  stats::rbinom(n, 1, true_p)
}))
ppc_calibration_residual(y, yrep)

}
\references{
Sailynoja, T., Johnson, A., Martin, R., & Vehtari, A. (2025).
Recommendations for visual predictive checks in Bayesian workflow.
arXiv:2503.01509.
}

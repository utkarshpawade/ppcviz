% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pit.R
\name{pit_kde}
\alias{pit_kde}
\title{Compute Visualization PIT Values for KDE}
\usage{
pit_kde(y, bw = "SJ", kernel = "gaussian", bounds = NULL)
}
\arguments{
\item{y}{Numeric vector of observed data.}

\item{bw}{Bandwidth selector passed to \code{\link[stats:density]{stats::density()}}. Default \code{"SJ"}
(Sheather-Jones). Can also be a numeric value.}

\item{kernel}{Kernel function passed to \code{\link[stats:density]{stats::density()}}. Default
\code{"gaussian"}.}

\item{bounds}{Optional numeric vector of length 2 specifying \code{c(lower, upper)}
bounds. Use \code{NA} or \code{Inf}/\code{-Inf} for one-sided bounds, e.g.
\code{c(0, NA)} for non-negative data.}
}
\value{
Numeric vector of PIT values in \eqn{[0,1]}, same length as \code{y}.
}
\description{
Computes probability integral transform (PIT) values of \code{y} with respect to
the kernel density estimate (KDE) fitted to \code{y} itself. This quantifies how
well a KDE density plot represents the empirical distribution of the data.
}
\details{
The approach is to: (1) fit a KDE using \code{\link[stats:density]{stats::density()}}, (2) numerically
integrate the density to get the CDF via cumulative trapezoid integration,
and (3) evaluate the CDF at each observation. The resulting PIT values
should be uniform on \eqn{[0,1]} if the KDE faithfully represents the data.

If \code{bounds} are provided, the data are reflected at the boundaries before
fitting the KDE (the "reflection method"), and the density is then cropped
to the bounded region and renormalised.
}
\examples{
set.seed(42)
y <- rnorm(200)
pit <- pit_kde(y)
hist(pit, breaks = 20, main = "PIT values — should be ~uniform")

# Bounded data: Beta(2, 5) on [0, 1]
y_beta <- rbeta(200, 2, 5)
pit_bounded <- pit_kde(y_beta, bounds = c(0, 1))
hist(pit_bounded, breaks = 20)

}
\references{
Sailynoja, T., Johnson, A., Martin, R., & Vehtari, A. (2025).
Recommendations for visual predictive checks in Bayesian workflow.
arXiv:2503.01509.
}
